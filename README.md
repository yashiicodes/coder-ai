# coder-ai
A command-line AI agent that navigates and debugs Python repositories using LLM function calling. It can inspect files, execute scripts, and iteratively resolve errors by feeding tool outputs back into the model, enabling multi-step autonomous debugging.
